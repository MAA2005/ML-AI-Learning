{
  "//": "Copy to relay.config.json (git-ignored) and edit. Provider ids must match those configured via env/keychain. Credentials never live here — only routing topology.",
  "chains": [
    {
      "name": "default",
      "strategy": "ordered",
      "providers": [{ "id": "openai" }, { "id": "openai-compat" }, { "id": "ollama" }]
    },
    {
      "name": "cheap-first",
      "strategy": "weighted",
      "providers": [
        { "id": "ollama", "weight": 3 },
        { "id": "openai-compat", "weight": 1 }
      ]
    },
    {
      "name": "spread",
      "strategy": "round-robin",
      "providers": [{ "id": "openai" }, { "id": "openai-compat" }]
    }
  ]
}
